name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - develop
jobs:
  build:
    uses: suppada/java-app/.github/workflows/maven_build.yml@main
    with:
      java_version: '21'
      enable_release: true